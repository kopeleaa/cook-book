methods:
  createIngredient:
    post:
      tags:
        - Ingredient
      summary: Create a new ingredient
      operationId: createIngredient
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/IngredientDTO'
      responses:
        200:
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IngredientDTO'

components:
  schemas:
    IngredientDTO:
      type: object
      properties:
        id:
          type: integer
          format: int64
        name:
          type: string
        description:
          type: string
        type:
          type: string
        quantity:
          type: number
          format: float
        unit:
          type: string
        expirationDate:
          type: string
          format: date
        tasteProfile:
          type: string
        inStock:
          type: boolean